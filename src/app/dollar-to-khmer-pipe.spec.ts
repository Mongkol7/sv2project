import { DollarToKhmerPipe } from './dollar-to-khmer-pipe';

describe('DollarToKhmerPipe', () => {
  it('create an instance', () => {
    const pipe = new DollarToKhmerPipe();
    expect(pipe).toBeTruthy();
  });
});
